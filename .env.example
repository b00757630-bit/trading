# Copier en .env et remplir. Ne jamais committer .env (déjà dans .gitignore).
# Optionnel : notifications Telegram (sinon le script tourne sans envoi)
# Créer un bot via @BotFather, récupérer le token.
# Pour CHAT_ID : envoyer un message au bot puis ouvrir :
#   https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_BOT_TOKEN=votre_token_bot
TELEGRAM_CHAT_ID=votre_chat_id

# Optionnel : gestion du risque (défauts 500€, 1%, 5€)
# CAPITAL_EUR=5000
# RISQUE_POURCENT=0.01
# RISQUE_EUROS=50
